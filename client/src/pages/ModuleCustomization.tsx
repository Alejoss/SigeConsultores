/**
 * Legacy route placeholder — la personalización vive en el panel de administración
 * (`CustomizeModulesPanel`) y en `/customize-modules` para gerentes (propósito/misión/visión).
 */
export default function ModuleCustomization() {
  return null;
}
